enum AirPressureType { qfe, qnh }

enum AirPressureDim { mbar, hPa, mmHg, inHg }

enum ConverterLimits { minValue, maxValue, roundDecimal }

enum WeightDim { kg, lbs, tonne }

enum HeightDim { m, ft }

enum DistanceDim { m, ft, km, nm, ml }

enum RunwayDistanceDim { m, ft }

enum HorizontalSpeedDim { kt, kmh, ms, mph }

enum VerticalSpeedDim { ftmin, ms }

enum TemperatureDim { c, f }

enum VolumeDim { liter, usGallon, imperialGallon, usQuart }

enum TimeDim { second, minute, hour }

enum DensityDim { kgPerLiter, kgPerCubicMeter, lbPerUsGallon }

enum VolumetricFlowDim {
  literPerHour,
  usGallonPerHour,
  imperialGallonPerHour,
}

enum MassFlowDim { kgPerHour, lbPerHour }
